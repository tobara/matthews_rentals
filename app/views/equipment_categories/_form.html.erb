<%= form_for(@equipment_category) do |f| %>
  <% if @equipment_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipment_category.errors.count, "error") %> prohibited this equipment_category from being saved:</h2>

      <ul>
      <% @equipment_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
